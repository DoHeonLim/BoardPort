# App / Security
# Production에서는 HTTPS origin만 허용합니다(경로·query·fragment 제외).
NEXT_PUBLIC_APP_URL=
# iron-session 요구사항에 따라 32자 이상이어야 합니다.
COOKIE_PASSWORD=
# Production 필수. COOKIE_PASSWORD와 다른 32자 이상의 임의 문자열을 사용합니다.
RATE_LIMIT_SALT=
# Vercel Cron Authorization에 사용할 12자 이상의 비밀값입니다.
CRON_SECRET=

# Database
# DATABASE_URL: Vercel 런타임용 Supabase transaction pooler URL(일반적으로 6543)
# DIRECT_URL: migration / Prisma CLI용 direct 또는 session pooler URL(일반적으로 5432)
DATABASE_URL=
DIRECT_URL=
# @prisma/adapter-pg는 URL의 connection_limit을 pg.Pool max로 사용하지 않으므로
# 아래 값을 명시적으로 읽습니다. 미설정 시 각각 5, 10000, 10000을 사용합니다.
DATABASE_POOL_MAX=5
DATABASE_CONNECTION_TIMEOUT_MS=10000
DATABASE_IDLE_TIMEOUT_MS=10000

# OAuth
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
KAKAO_CLIENT_ID=
KAKAO_CLIENT_SECRET=
KAKAO_REDIRECT_URI=

# Supabase Realtime
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLIC_KEY=
# Realtime REST 발신용 sb_secret_ 키와 BoardPort 단기 JWT 서명용 ES256 private JWK
SUPABASE_SECRET_KEY=
# 원문 JSON도 허용하지만 개행·따옴표 문제를 피하려면 JWK JSON을 base64 한 줄로 저장
SUPABASE_REALTIME_SIGNING_KEY_JWK=

# Cloudflare Media / Webhook
NEXT_PUBLIC_CLOUDFLARE_ACCOUNT_HASH=
NEXT_PUBLIC_CLOUDFLARE_STREAM_DOMAIN=
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_WEBHOOK_SECRET=
CLOUDFLARE_STREAM_WEBHOOK_SECRET=
# Cloudflare Stream signing key API 응답의 key id와 base64 private JWK (server-only secret)
CLOUDFLARE_STREAM_SIGNING_KEY_ID=
CLOUDFLARE_STREAM_SIGNING_KEY_JWK=

# SMS (CoolSMS)
COOLSMS_API_KEY=
COOLSMS_API_SECRET=
COOLSMS_SENDER_NUMBER=

# Email (Resend)
RESEND_API_KEY=

# Push
NEXT_PUBLIC_VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=

# Maps
NEXT_PUBLIC_KAKAO_MAP_API_KEY=
